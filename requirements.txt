# Core
docker==7.1.0
PyGithub==2.5.0
requests==2.32.3
pyyaml==6.0.2
jinja2==3.1.5
python-dotenv>=1.1.0
fastapi==0.137.2
uvicorn[standard]==0.34.0
httpx==0.28.1

# LLM clients
ollama==0.4.7
anthropic==0.43.0
openai==1.59.2

# Security scanners (Python wrappers)
bandit==1.8.3

# Testing
pytest==8.3.4
pytest-cov==6.0.0
pytest-asyncio==0.25.0
pytest-json-report==1.5.0  # required by agent/testing/runner.py's --json-report flag —
                            # target Python repos scanned inside the sandbox get this
                            # for free since the sandbox reuses the control-plane image

# Evaluation
pandas==2.2.3
matplotlib==3.10.0
njsscan==0.4.3  # evaluation/run_extended_scan.py baseline; pinned to match the
                # version cited in Paper/paper/sections/05_results.tex — previously
                # not pinned anywhere, so a fresh install could silently diverge
                # from the reported JS-scanner results with no record of it.
